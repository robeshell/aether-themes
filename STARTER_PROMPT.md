# Aether 正式建站提示词

这是给第一次使用 Aether、准备创建自己网站的用户准备的复制即用提示词。

它用于创建正式网站，不是主题包验收测试。测试主题包请使用 [`SMOKE_TEST_PROMPT.md`](./SMOKE_TEST_PROMPT.md)。

## 使用方法

1. 新建或打开一个专门放网站的空目录。
2. 用 AI 工具打开这个网站目录，而不是打开 Aether 主题仓库。
3. 复制下面的提示词发送给 AI。
4. AI 完成后，打开它提供的本地地址预览网站。

如果你还没有网站目录，请先手动新建并打开一个空目录；不要把 Aether 主题仓库当作网站目录。

## 复制下面的提示词

```text
请你作为一个新手友好的网站搭建助手，带我从零创建一个使用 Aether 主题的正式个人网站。

参考仓库：
https://github.com/robeshell/aether-themes

重要规则：

- 当前工作目录就是我的正式网站项目根目录。
- 所有网站文件都直接创建在当前目录，或当前目录的必要子目录中。
- 不要创建 aether-ai-smoke-test、smoke-test、test-site、site 等测试目录。
- 不要使用绝对路径，不要访问或修改当前目录之外的文件。
- 不要把网站创建到 Aether 主题仓库的子目录中。
- 如果当前目录看起来是 Aether 主题仓库（例如 package.json 的 name 是 aether-themes，或存在 Aether 的 AGENTS.md），请立即停止，不要创建任何文件，并告诉我需要切换到空的网站目录后重新运行。
- 如果当前目录已有文件或已有网站，先列出会受影响的文件并暂停询问我；不要覆盖、删除或迁移现有内容。
- Aether 只作为依赖和参考使用，不要修改 Aether 主题仓库源码，不要复制 W.Site 的代码或内容。
- 不要删除任何文件，不要编造我的个人经历、履历或成就。
- 不要提交 GitHub，不要发布 npm，也不要部署线上网站，除非我之后明确要求。
- 每一步先用一句简单中文说明要做什么，再执行命令。
- 如果遇到登录、密码、验证码或权限问题，请暂停并告诉我。

开始前：先了解我的需求

在执行任何命令、创建任何文件之前，请一次性向我提问以下问题，并等待我回答：

1. 站点名称是什么？如果还没想好，可以使用临时名称。
2. 站点的一句话介绍是什么？如果没有，请提供一个简短的默认选项让我确认。
3. 希望显示什么作者名或署名？可以留空。
4. 准备记录哪些内容？可多选：文章、笔记、游记、照片、音乐，或其他栏目。
5. 想启用哪些 Aether 主题？如果不确定，请先推荐 minimal，并询问是否再加入其他主题。
6. 默认打开哪个主题？默认主题必须属于启用的主题。
7. 网站使用什么语言？默认使用中文。
8. 是否需要示例文章和示例媒体来检查排版？默认需要。

如果我对某项没有想法，请给出一个清楚的默认值，不要替我编造个人经历、履历或成就。
收到回答后，请先用一段简短摘要复述建站方案，并等待我回复“开始创建”或类似确认。没有确认之前，不要执行后面的命令，也不要创建或修改文件。

一、确认工作目录

确认当前目录不是 Aether 主题仓库，也不是其他已有项目；确认它可以作为正式网站根目录。
请执行 pwd 和 git status（如果当前目录是 Git 仓库），并报告检查结果。
请阅读 Aether 仓库中的以下文件；如果只能通过网页链接访问，请阅读对应的 GitHub 文件：

- AGENTS.md
- README.md
- skills/aether-blog/SKILL.md
- THEME_CONTRACT.md
- aether.config.example.mjs

二、安装并配置 Aether

直接在当前目录从零创建一个 Astro 网站。不要再创建额外的 test 或 site 外层目录。

要求：

- 使用 Astro
- 使用 npm 安装 aether-themes
- 不要复制 W.Site 的代码
- 不要依赖其他个人网站的内容
- 根据 aether.config.example.mjs 创建当前网站的 aether.config.mjs
- 只启用我在前面选择的主题
- defaultTheme 必须是已启用的主题
- 使用 aether-themes 命令生成 src/styles/aether-themes.css
- 让主题切换器读取 aether.config.mjs 中的 themes 数组

如果需要安装依赖，请只修改当前网站自己的 package.json 和 lockfile。

三、创建页面和内容

请创建：

- 首页
- 文章列表页
- 文章详情页
- 关于页面
- 主题切换器

如果我确认需要示例内容，请创建清楚标注为“示例”的文章，测试以下内容：

1. 普通长文
2. 图片
3. 视频
4. 音乐播放器
5. 代码块
6. 数学公式
7. 图片影集
8. 引用和列表
9. 表格

没有真实媒体文件时，请使用清楚标注的示例占位内容，不要下载或使用有版权风险的素材。

四、检查页面

请检查所有已启用的主题：

- 主题切换是否正常
- 切换器是否只显示已启用的主题
- 文章文字层级是否清晰
- 图片、视频、音频、代码、公式和影集是否能显示
- 桌面端是否正常
- 390px 宽度的手机端是否横向溢出
- 是否有控制台错误
- 是否尊重系统的减少动画设置

五、构建并启动

在当前目录执行：

npm run build

构建成功后，再执行：

npm run dev -- --host 0.0.0.0

告诉我本地访问地址，并保持开发服务器运行。

六、输出报告

请用简单中文报告：

- 创建了哪些相对路径
- 执行过哪些命令
- 安装的 aether-themes 版本
- 启用了哪些主题
- 创建了哪些页面和文章
- 图片、视频、音频、代码、公式和影集是否正常
- 桌面端和手机端是否正常
- npm run build 是否成功
- 发现了哪些问题
- 哪些问题需要我手动处理
```

## 通过标准

一次合格的正式建站至少应该满足：

- 网站文件位于当前工作目录，而不是 `aether-ai-smoke-test` 或其他测试目录
- 如果参考的是 Aether 仓库，Aether 仓库源码没有被修改
- `aether-themes` 可以从 npm 安装
- 配置只加载用户选择的主题
- 主题切换器与配置保持一致
- 内容模块可以正常渲染
- 桌面端和手机端没有横向溢出
- `npm run build` 成功

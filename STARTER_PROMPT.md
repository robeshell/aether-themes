# Aether 初始安装与构建提示词

这是给第一次使用 Aether 的用户准备的复制即用提示词。

使用方法：

1. 用 AI 工具打开 Aether 仓库。
2. 允许 AI 使用当前目录中的终端。
3. 复制下面的提示词发送给 AI。
4. AI 完成后，打开它提供的本地地址预览网站。

## 复制下面的提示词

```text
请你作为一个新手友好的网站搭建助手，带我从零创建一个使用 Aether 主题的个人博客。

重要规则：

- 所有操作只能在当前工作目录或它的子目录中完成。
- 不要使用绝对路径。
- 不要访问或修改当前目录之外的文件。
- 当前仓库只作为参考，不要修改仓库源码。
- 如果当前仓库存在未提交的修改，请保留，不要覆盖。
- 不要删除任何文件。
- 不要提交 GitHub，不要发布 npm，也不要发布线上网站。
- 每一步先用一句简单中文说明要做什么，再执行命令。
- 如果遇到登录、密码、验证码或权限问题，请暂停并告诉我。

一、确认工作目录

请先执行 pwd 和 git status，确认当前打开的是 Aether 仓库。
请阅读这些文件：

- AGENTS.md
- README.md
- skills/aether-blog/SKILL.md
- THEME_CONTRACT.md
- aether.config.example.mjs

二、创建全新的测试项目

不要把测试网站创建在仓库根目录。
请在当前目录下创建：

./aether-ai-smoke-test

如果这个目录已经存在，不要删除它，改用：

./aether-ai-smoke-test-2

然后在这个新目录中创建：

./site

之后只在 ./site 中创建和修改测试网站。

三、安装并配置 Aether

在 ./site 中从零创建一个 Astro 网站。

要求：

- 使用 Astro
- 使用 npm 安装 aether-themes
- 不要复制 W.Site 的代码
- 不要依赖我的个人网站内容
- 从仓库中的 aether.config.example.mjs 创建 ./site/aether.config.mjs
- 只启用 minimal 和 persona 两个主题
- defaultTheme 设置为 minimal
- 使用 aether-themes 命令生成 src/styles/aether-themes.css
- 让主题切换器读取 aether.config.mjs 中的 themes 数组

如果需要安装依赖，请使用当前项目自己的 package.json，不要修改仓库根目录的 package.json。

四、创建测试页面

请创建：

- 首页
- 文章列表页
- 文章详情页
- 关于页面
- 主题切换器

请创建几篇测试文章，测试以下内容：

1. 普通长文
2. 图片
3. 视频
4. 音乐播放器
5. 代码块
6. 数学公式
7. 图片影集
8. 引用和列表
9. 表格

没有真实媒体文件时，请使用清楚标注的示例占位内容，不要下载或使用有版权风险的素材。
不要编造我的个人经历、履历或成就。

五、检查页面

请分别检查 minimal 和 persona 主题：

- 主题切换是否正常
- 切换器是否只显示已启用的主题
- 文章文字层级是否清晰
- 图片、视频、音频、代码、公式和影集是否能显示
- 桌面端是否正常
- 390px 宽度的手机端是否横向溢出
- 是否有控制台错误
- 是否尊重系统的减少动画设置

六、构建并启动

在 ./site 中执行：

npm run build

构建成功后，再执行：

npm run dev -- --host 0.0.0.0

告诉我本地访问地址，并保持开发服务器运行。

七、输出测试报告

请用简单中文报告：

- 创建了哪些相对路径
- 执行过哪些命令
- 安装的 aether-themes 版本
- 启用了哪些主题
- 创建了哪些页面和文章
- 图片、视频、音频、代码、公式和影集是否正常
- 桌面端和手机端是否正常
- npm run build 是否成功
- 发现了哪些问题
- 哪些问题需要我手动处理
```

## 通过标准

一次合格的初始构建至少应该满足：

- 测试网站位于当前目录下的新子目录中
- Aether 仓库源码没有被修改
- `aether-themes` 可以从 npm 安装
- 配置只加载用户选择的主题
- 主题切换器与配置保持一致
- 内容模块可以正常渲染
- 桌面端和手机端没有横向溢出
- `npm run build` 成功
